# 2026-08-06

## IO 电平反转功能

为 IOWidget 添加 DI/DO 电平反转设置功能：
- ConfigMgr: 新增 m_diInvert/m_doInvert QMap, loadIODescriptions/saveIODescriptions 支持 diInvert/doInvert key, 新增 setIOConfig() 合并保存描述+反转
- GtsMgr: onRefreshTimeout 中读取 DI/DO 后应用反转再 emit, 确保显示值正确
- IOWidget: onDOCellClicked 写 DO 前应用反转, onDescriptionEdited 改为自定义对话框(描述+反转 checkbox)
- JSON 格式: ioDescription.json 版本升至 2, 新增 diInvert/doInvert 对象, 向后兼容
- 5 个文件修改: GtsConfigMgr.h/cpp, GtsMgr.cpp, IOWidget.h/cpp
- 编码: 3 个 .cpp 文件确认 UTF-8+BOM, 0 U+FFFD

## DO 反转逻辑下沉到 GtsMgr

- GtsMgr: 新增 setDOValue(const DO&)，内部 applyInvert XOR 后调用 ioMgr->setXxxDO()，反转逻辑统一在一处
- IOWidget: onDOCellClicked 简化为三段 toggle + 一行 m_gtsMgr->setDOValue(m_doState), 删除所有 XOR 循环和 ioMgr 直接调用
