# 2026-07-03

## 将审查问题迁移至 GTS.md
- 把 07-02 审查报告中的 3 个 P0 + 4 个 P1 问题写入 GTS.md，位于"其他"和"复杂插补"之间
- 使用 `- [ ]` 勾选格式，修复后改为 `- [x]`
- 每个问题含编号、位置、简要方案

## GTS.md 全文格式统一为 checkbox 风格
- 原有 `## xxx √` 改为 `- [x] xxx`
- 原有 `## xxx` (无 √) 改为 `- [ ] xxx`
- `###` 子项改为缩进的 `- [ ]`
- 标题层级保持 `#` 不变，去掉了标题尾部的 `----` 分隔线

## P0-01 部分修复：startTrapMotion 解耦 QMessageBox
- MotionMgr.cpp → startTrapMotion()：5 处 QMessageBox::warning 替换为 emit errorOccurred(profile, -1, msg)
- startJogMotion 仍有 3 处 QMessageBox，下次处理
- 对应更新 GTS.md：P0-01 拆分子项标记进度

## P0-01 闭环 + P0-02 修复
- P0-01: startJogMotion 3 处 QMessageBox → emit errorOccurred，移除 #include <QMessageBox>
- P0-02: startTrapMotion 循环 Trap 从 for+waitMotionDone 改为 signal 驱动异步状态机
  - MotionMgr.h: +TrapCycleState 结构体、+cancelTrapCycle()、+onTrapCycleStep() 槽、+m_trapCycle 成员
  - MotionMgr.cpp: 构造函数加 connect(motionDone→onTrapCycleStep)、改写循环逻辑、新增 cancelTrapCycle + onTrapCycleStep
- GTS.md 对应标记 [x]

## P0-02 修正：signal 驱动改为递归自调度
- 问题：上次 connect(motionDone→onTrapCycleStep) 永远没人 emit motionDone，循环运动无法执行后续步骤
- 修复：
  - 构造函数：删除无用的 connect(motionDone → onTrapCycleStep)
  - startTrapMotion：cycleTimes>0 时不再调 singleTrapMotion 发第一步，改为直接调 onTrapCycleStep(profile)
  - onTrapCycleStep 重写：先递增 currentStep → 检查完成 → 调 singleTrapMotion 发运动 → waitMotionDone → delay → QMetaObject::invokeMethod(Qt::QueuedConnection) 递归调度自己
  - cancelTrapCycle 不变，设置 active=false 即可在下一轮 onTrapCycleStep 中中断
- GTS.md 同步更新方案描述
